#include <stdio.h>
int main()
{
    int number = 101;
    int x = number / 2;
    int y = x * 2;
    if (number / 2 && number == y)
    {
        printf("the number is even\n");
    }
    else
    {
        printf("the number is odd\n");
    }
}