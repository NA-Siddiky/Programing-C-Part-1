#include <stdio.h>
int main()
{
    char ch;
    printf("Enter the name of the first letter: ");
    ch = getchar();
    printf("The first letter of your name is: %c\n", ch);
    return 0;

    // char c = "N";
    // printf("Enter the name of the first letter: ");
    // c = getchar();
    // printf("The first letter of your name is: %c\n", c);
    // return 0;
}