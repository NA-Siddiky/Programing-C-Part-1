#include <stdio.h>
int main()
{
    double a, b, sum;
    scanf("%lf", &a);
    scanf("%lf", &b);
    sum = a + b;

    printf("a= %lf\n", a);
    printf("b= %lf\n", b);
    printf("sum is : %lf\n", sum);
    return 0;
}